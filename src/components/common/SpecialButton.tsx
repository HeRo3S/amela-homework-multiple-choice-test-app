import { Button, styled } from "@mui/material";

const StyledSpecialButton = styled(Button)({
  backgroundColor: "#9F9D9F",
  borderRadius: "41px",
  fontWeight: "700",
});

export default StyledSpecialButton;
